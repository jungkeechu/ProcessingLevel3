// interaction(mouse, keyboard, image)
// https://processing.org/reference/#input

// MOUSE 
// mouseX, mouseY
// mousePressed
// mousePressed()
// pmouseX, pmouseY
// mouseButton

// EASING

// IMAGE

// KEYBOARD 

void setup() {
  size(600, 600);
  stroke(255);
}

void draw() {
  background(128);
  ellipse(mouseX, mouseY, 100, 100);
}
